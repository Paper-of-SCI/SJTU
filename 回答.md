# 最小二乘法完整推导例子

## 问题

已知三个点 $(1,2)$，$(2,3)$，$(3,3)$，找一条最拟合的直线 $y = ax + b$。

---

## Step 1：列方程组

把三个点代入 $y = ax + b$：

$$
\begin{cases}
a \cdot 1 + b = 2 \\
a \cdot 2 + b = 3 \\
a \cdot 3 + b = 3
\end{cases}
$$

3个方程，2个未知数 $a, b$，方程比未知数多 → **超定方程组，无精确解**。

---

## Step 2：写成矩阵形式 $Ax = b$

把系数抽出来：

$$
\underbrace{\begin{bmatrix}1 & 1 \\ 2 & 1 \\ 3 & 1\end{bmatrix}}_{A} \underbrace{\begin{bmatrix}a \\ b\end{bmatrix}}_{x} = \underbrace{\begin{bmatrix}2 \\ 3 \\ 3\end{bmatrix}}_{b}
$$

---

## Step 3：目标函数

找 $x$ 使残差平方和最小：

$$
\min_{x} \|Ax - b\|^2 = \min_{a,b} \left[(a+b-2)^2 + (2a+b-3)^2 + (3a+b-3)^2\right]
$$

---

## Step 4：对目标函数求导，令导数为零

展开 $\|Ax - b\|^2$：

$$
\|Ax - b\|^2 = (Ax-b)^T(Ax-b) = x^TA^TAx - 2b^TAx + b^Tb
$$

对 $x$ 求导：

$$
\frac{\partial}{\partial x}\|Ax - b\|^2 = 2A^TAx - 2A^Tb = 0
$$

整理得**正规方程（Normal Equation）**：

$$
A^TAx = A^Tb
$$

$$
x = (A^TA)^{-1}A^Tb
$$

---

## Step 5：计算 $A^TA$

$$
A^T = \begin{bmatrix}1 & 2 & 3 \\ 1 & 1 & 1\end{bmatrix}
$$

$$
A^TA = \begin{bmatrix}1 & 2 & 3 \\ 1 & 1 & 1\end{bmatrix} \begin{bmatrix}1 & 1 \\ 2 & 1 \\ 3 & 1\end{bmatrix} = \begin{bmatrix}1^2+2^2+3^2 & 1+2+3 \\ 1+2+3 & 1+1+1\end{bmatrix} = \begin{bmatrix}14 & 6 \\ 6 & 3\end{bmatrix}
$$

---

## Step 6：计算 $A^Tb$

$$
A^Tb = \begin{bmatrix}1 & 2 & 3 \\ 1 & 1 & 1\end{bmatrix} \begin{bmatrix}2 \\ 3 \\ 3\end{bmatrix} = \begin{bmatrix}1\cdot2 + 2\cdot3 + 3\cdot3 \\ 1\cdot2 + 1\cdot3 + 1\cdot3\end{bmatrix} = \begin{bmatrix}17 \\ 8\end{bmatrix}
$$

---

## Step 7：计算 $(A^TA)^{-1}$

对于 $2\times2$ 矩阵 $\begin{bmatrix}p & q \\ r & s\end{bmatrix}$，逆矩阵为 $\frac{1}{ps-qr}\begin{bmatrix}s & -q \\ -r & p\end{bmatrix}$

$$
\det(A^TA) = 14 \times 3 - 6 \times 6 = 42 - 36 = 6
$$

$$
(A^TA)^{-1} = \frac{1}{6}\begin{bmatrix}3 & -6 \\ -6 & 14\end{bmatrix} = \begin{bmatrix}0.5 & -1 \\ -1 & \frac{7}{3}\end{bmatrix}
$$

---

## Step 8：求解 $x = (A^TA)^{-1}A^Tb$

$$
x = \begin{bmatrix}0.5 & -1 \\ -1 & \frac{7}{3}\end{bmatrix} \begin{bmatrix}17 \\ 8\end{bmatrix} = \begin{bmatrix}0.5\times17 + (-1)\times8 \\ (-1)\times17 + \frac{7}{3}\times8\end{bmatrix} = \begin{bmatrix}8.5 - 8 \\ -17 + \frac{56}{3}\end{bmatrix} = \begin{bmatrix}0.5 \\ \frac{5}{3}\end{bmatrix}
$$

---

## 结果

$$
a = 0.5, \quad b = \frac{5}{3} \approx 1.67
$$

最拟合直线为：

$$
y = 0.5x + \frac{5}{3}
$$

## 验证（代入三个点看残差）

| 点 | 真实 $y$ | 预测 $\hat{y}$ | 残差 |
|----|----------|----------------|------|
| $x=1$ | $2$ | $0.5+1.67=2.17$ | $-0.17$ |
| $x=2$ | $3$ | $1.0+1.67=2.67$ | $+0.33$ |
| $x=3$ | $3$ | $1.5+1.67=3.17$ | $-0.17$ |

残差不为零（因为超定），但平方和已经是所有直线里最小的了。
