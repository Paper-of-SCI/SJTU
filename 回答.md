可以。这个“估计深度”本质上就是：

  我不知道像素 u 对应的物体在 2m、3m 还是 4m；
  那我就假设几个深度，把这个点投到其他视角里，看哪个深度下多视角特征最匹配。

  ———

  ## 最简单例子：两张图估一个像素的深度

  有两张图：

  参考图 I1
  另一张图 I2

  参考图里有一个像素：

  u = (100, 80)

  我们不知道它的深度，所以假设三个候选深度：

  d1 = 2m
  d2 = 3m
  d3 = 4m

  ———

  ## 第一步：每个深度都反投影成 3D 点

  对每个候选深度：

  $$
  \mathbf{x}_k = \Pi_1^{-1}(u, d_k)
  $$

  得到三个可能的 3D 点：

  x1：沿着像素 u 的 ray 走 2m
  x2：沿着像素 u 的 ray 走 3m
  x3：沿着像素 u 的 ray 走 4m

  ———

  ## 第二步：把这三个 3D 点投影到第二张图

  用第二张相机的投影函数：

  $$
  u'_k = \Pi_2(\mathbf{x}_k)
  $$

  比如得到：

  d1 = 2m → u'_1 = (130, 82)
  d2 = 3m → u'_2 = (120, 81)
  d3 = 4m → u'_3 = (113, 80)

  意思是：

  如果真实深度是 2m，那么这个点在 I2 里应该出现在 (130,82)
  如果真实深度是 3m，那么应该出现在 (120,81)
  如果真实深度是 4m，那么应该出现在 (113,80)

  ———

  ## 第三步：比较特征是否匹配

  从两张图提特征：

  F1(u)
  F2(u'_1)
  F2(u'_2)
  F2(u'_3)

  比较相似度：

  $$
  s_k =
  \mathrm{sim}
  (
  F_1(u), F_2(u'_k)
  )
  $$

  假设算出来：

  d1=2m：相似度 0.30
  d2=3m：相似度 0.92
  d3=4m：相似度 0.45

  那说明：

  深度 3m 时，I1 里的这个点和 I2 里的对应点最像。

  所以预测：

  $$
  d = 3m
  $$

  ———

  ## 第四步：得到深度概率

  实际网络不会硬选，而是做 softmax：

  相似度: [0.30, 0.92, 0.45]
  softmax: [0.25, 0.46, 0.29]

  这就是：

  $$
  P(d_k|u)
  $$

  然后深度可以是加权平均：

  $$
  d(u)

  \sum_k P(d_k|u)d_k
  $$

  或者取最大概率：

  $$
  d(u)=d_{\arg\max_k P(d_k|u)}
  $$

  ———

  ## 第五步：用深度反投影成 Gaussian center

  如果最后选：

  d = 3m

  那就：

  $$
  \mathbf{x}

  \Pi_1^{-1}(u,3m)
  $$

  这个 (\mathbf{x}) 就可以作为候选 Gaussian center / anchor。

  后面再预测：

  offset
  scale
  rotation
  opacity
  color

  ———

  ## 大白话总结

  同一个像素对应一条射线。
  我沿这条射线猜几个深度。
  每个深度都会在另一张图里落到不同位置。
  哪个位置的特征最像参考图里的这个像素，
  哪个深度就更可能是真的。

  这就是 cost volume / depth estimation 的核心。