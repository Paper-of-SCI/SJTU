# 基于低容量 3D Medium Field 的水下 3DGS 渲染公式

## 1. 核心想法

现有 SeaSplat 的水下成像方式可以理解为：先用 3DGS 渲染出一个像素级的无水颜色 \(J\) 和深度 \(Z\)，然后用深度函数生成 attenuation 和 backscatter：

$$
I(x)
=
J(x)\cdot A(Z(x)) + B(Z(x))
$$

其中 \(x\) 是图像像素位置，\(I(x)\) 是最终水下图像颜色，\(J(x)\) 是 3DGS 直接渲染出的颜色，\(Z(x)\) 是 3DGS 渲染出的深度。

这个模型的优点是简单、稳定、参数少；但它隐含了一个较强假设：水体影响主要只由最终深度 \(Z(x)\) 决定。也就是说，只要两个像素深度相同，它们的 attenuation 和 backscatter 就倾向于相似。

真实水下环境里，水体往往不是空间均匀的。某些区域可能更浑浊，某些区域可能更清澈；不同 ray 即使终点深度相同，也可能穿过不同的水体区域。因此，更合理的建模方式是：不要给每个物体 Gaussian 直接加自由水体参数，而是额外建立一个低容量的 3D medium field，让水体参数存在于 3D 空间中。

一句话概括：

**3DGS 表示场景表面和颜色，额外用少量 3D medium Gaussians 或低容量 MLP 表示空间非均匀水体，然后沿每条 camera ray 对水体影响进行积分。**

## 2. 原版 3DGS 的像素颜色

对于一条相机 ray：

$$
r(t)=o+td
$$

其中：

- \(r(t)\)：ray 上距离相机 \(t\) 处的 3D 点。
- \(o\)：相机中心。
- \(d\)：ray 的单位方向。
- \(t\)：从相机中心出发沿 ray 的距离。

原版 3DGS 在像素 \(x\) 上的颜色可以写成 alpha compositing：

$$
J(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
$$

其中：

- \(J(r)\)：没有显式水体介质时，3DGS 在这条 ray 上渲染出的 RGB 颜色。
- \(i\)：参与当前像素渲染的第 \(i\) 个 scene Gaussian，通常按深度从近到远排序。
- \(c_i\)：第 \(i\) 个 scene Gaussian 的颜色，可以来自 SH 球谐颜色或 RGB 参数。
- \(\alpha_i(r)\)：第 \(i\) 个 scene Gaussian 对当前 ray/像素的有效 opacity 贡献。它由 Gaussian 自身 opacity 和投影到屏幕后的 footprint 共同决定。
- \(T_i^{gs}(r)\)：3DGS 自身的前向透射率，表示第 \(i\) 个 Gaussian 前面的 Gaussian 已经遮挡后，当前 Gaussian 的贡献还能透过多少。

3DGS 的前向透射率为：

$$
T_i^{gs}(r)
=
\prod_{j<i}
\left(1-\alpha_j(r)\right)
$$

所以：

$$
T_i^{gs}(r)\alpha_i(r)c_i
$$

表示第 \(i\) 个 Gaussian 对最终像素颜色的实际贡献。

如果需要解释 3DGS 渲染出的深度，可以写成一个加权聚合的近似形式：

$$
Z(r)
\approx
\frac{
\sum_i T_i^{gs}(r)\alpha_i(r)t_i
}{
\sum_i T_i^{gs}(r)\alpha_i(r)+\epsilon
}
$$

其中：

- \(Z(r)\)：当前 ray 的渲染深度。
- \(t_i\)：第 \(i\) 个 Gaussian 在当前相机坐标系或当前 ray 上的有效深度。
- \(\epsilon\)：防止除零的小常数。

这里的 \(t_i\) 不是一个额外网络预测出来的值，而是由第 \(i\) 个 Gaussian 的 3D 位置和当前相机位姿决定的有效深度。

需要注意：后文介质积分中的 \(t\) 表示沿单位 ray 的路径长度，而不只是相机坐标系的 \(z\)-depth。因此严格来说，\(t_i\) 应该和 ray 参数 \(r(t)=o+td\) 保持一致。

如果相机中心为 \(o\)，当前像素的单位 ray 方向为 \(d\)，第 \(i\) 个 Gaussian 的世界坐标中心为 \(\mu_i\)，则可以用 Gaussian 中心在 ray 方向上的投影作为近似深度：

$$
t_i
=
d^\top(\mu_i-o)
$$

其中 \(t_i>0\) 表示该 Gaussian 位于相机前方。

如果第 \(i\) 个 Gaussian 的世界坐标中心为：

$$
\mu_i
=
\begin{bmatrix}
x_i \\
y_i \\
z_i
\end{bmatrix}
$$

当前相机外参为 \(R,t\)，则相机坐标系下的位置为：

$$
\mu_i^{cam}
=
R\mu_i+t
=
\begin{bmatrix}
X_i^{cam} \\
Y_i^{cam} \\
Z_i^{cam}
\end{bmatrix}
$$

于是可将：

$$
t_i
\approx
\frac{Z_i^{cam}}{d_z^{cam}}
$$

其中 \(d_z^{cam}\) 是当前像素单位 ray 在相机坐标系下的 \(z\) 分量。只有在中心视线附近，或实现中明确把 ray 参数近似为 camera z-depth 时，才可以进一步近似为：

$$
t_i \approx Z_i^{cam}
$$

实际 CUDA rasterizer 的 depth 输出可能有具体实现细节，但如果后续要做物理意义上的水体路径积分，最好使用沿 ray 的 metric distance，避免图像边缘像素的水体路径被低估。

## 3. SeaSplat 的水下成像形式

SeaSplat 使用的水下成像公式可以概括为：

$$
I(r)
=
J(r)\cdot A(Z(r)) + B(Z(r))
$$

其中：

- \(I(r)\)：最终水下观测颜色，也就是模型输出后和真实水下图像 GT 比较的像素颜色。
- \(J(r)\)：3DGS Gaussian renderer 直接渲染出的颜色。
- \(Z(r)\)：3DGS 渲染出的深度。
- \(A(Z(r))\)：由深度决定的 attenuation，表示物体颜色穿过水体后还剩多少。
- \(B(Z(r))\)：由深度决定的 backscatter，表示水体沿视线方向散射进相机的颜色。

常见写法是：

$$
A(Z)
=
\exp(-\beta_D Z)
$$

$$
B(Z)
=
B_\infty\left(1-\exp(-\beta_B Z)\right)
$$

其中：

- \(\beta_D\)：直接光衰减系数。
- \(\beta_B\)：后向散射系数。
- \(B_\infty\)：无穷远处的水体颜色或环境散射颜色。

因此 SeaSplat 的整体形式为：

$$
I(r)
=
J(r)\cdot \exp(-\beta_D Z(r))
+
B_\infty\left(1-\exp(-\beta_B Z(r))\right)
$$

这个形式本质上是：**先把所有 scene Gaussians 混成一个像素颜色 \(J(r)\)，再用一个像素深度 \(Z(r)\) 对整个像素统一施加水体影响。**

## 4. 现有形式的局限

SeaSplat 的 depth-only 水体形式有三个关键限制。

第一，水体参数缺少 3D 空间位置感。两个 ray 如果最终深度相同，但穿过的水体区域不同，SeaSplat 仍然倾向于给出相似 attenuation 和 backscatter。

第二，所有 Gaussian 的颜色先被合成为一个 \(J(r)\)，然后共用同一个 \(A(Z)\)。但 3DGS 是多层 alpha compositing，ray 上不同 Gaussian 位于不同深度。近处 Gaussian 和远处 Gaussian 的光线实际穿过水体长度不同，应该受到不同水体透射率影响。

第三，如果给每个 scene Gaussian 都直接加独立水体参数，会造成参数过多和解释混乱。scene Gaussian 的颜色、opacity、水体 attenuation、backscatter 可能互相抢解释权，导致可辨识性变差。

因此更稳妥的思路是：保持 scene Gaussian 仍然负责场景几何和颜色，另建一个低容量 3D medium field 负责空间水体分布。

## 5. 新的 3D Medium Field 表示

我们引入一个定义在 3D 空间中的水体消光密度场：

$$
\beta(x)
$$

其中：

- \(x\in\mathbb{R}^3\)：3D 空间位置。
- \(\beta(x)\)：该位置处的水体消光密度，数值越大表示该处水体越浑浊、吸收或散射越强。

更严格地说，\(\beta(x)\) 可以理解为 extinction density：

$$
\beta(x)
=
\beta_{\mathrm{ext}}(x)
$$

它包含吸收和 out-scattering 对直接光造成的总衰减。后文如果没有特别说明，\(\beta(x)\) 都表示这个总消光密度。

### 5.1 MLP 形式

一种选择是用低容量 MLP 表示：

$$
\beta(x)
=
\mathrm{softplus}(f_\theta(x))
$$

其中：

- \(f_\theta\)：参数为 \(\theta\) 的小型 MLP。
- \(\mathrm{softplus}(\cdot)\)：保证输出非负。
- \(\theta\)：medium field 的可学习参数。

如果要建模 RGB 三通道不同衰减，可以写成：

$$
\boldsymbol{\beta}(x)
=
\mathrm{softplus}(f_\theta(x))
\in \mathbb{R}_+^3
$$

其中 \(\boldsymbol{\beta}(x)\) 的三个分量分别对应 R/G/B 通道的水体消光密度。

### 5.2 Medium Gaussian 形式

更贴近 3DGS 表示的一种选择是用少量 medium Gaussians：

$$
\beta(x)
=
\sum_k w_k G_k(x)
$$

其中：

- \(k\)：第 \(k\) 个 medium Gaussian。
- \(G_k(x)\)：第 \(k\) 个 medium Gaussian 在位置 \(x\) 的响应值。
- \(w_k\)：第 \(k\) 个 medium Gaussian 的非负强度。

具体地：

$$
G_k(x)
=
\exp
\left(
-\frac{1}{2}
(x-m_k)^T
\Sigma_k^{-1}
(x-m_k)
\right)
$$

其中：

- \(m_k\)：第 \(k\) 个 medium Gaussian 的 3D 中心。
- \(\Sigma_k\)：第 \(k\) 个 medium Gaussian 的 3D 协方差，控制它在空间中的范围和形状。
- \(w_k\)：该 medium Gaussian 对水体密度的贡献强度。

如果建模 RGB 通道衰减，可以写成：

$$
\boldsymbol{\beta}(x)
=
\sum_k \boldsymbol{w}_k G_k(x)
$$

其中：

- \(\boldsymbol{w}_k\in\mathbb{R}_+^3\)：第 \(k\) 个 medium Gaussian 对 RGB 三通道的衰减强度。

这个形式的优点是参数量低、空间局部性强，并且和 3DGS 的空间表示天然兼容。

## 6. 沿 ray 的水体透射率

对于一条 ray \(r(t)\)，从相机到深度 \(t\) 的 medium transmittance 定义为：

$$
T_m(t)
=
\exp
\left(
-\int_0^t
\beta(r(s))ds
\right)
$$

其中：

- \(T_m(t)\)：从 \(r(t)\) 处到相机之间的水体透射率。
- \(\beta(r(s))\)：ray 上位置 \(r(s)\) 处的水体消光密度。
- \(\int_0^t \beta(r(s))ds\)：从相机到深度 \(t\) 这段路径上累积的水体衰减。

如果使用 RGB 三通道衰减，则：

$$
\boldsymbol{T}_m(t)
=
\exp
\left(
-\int_0^t
\boldsymbol{\beta}(r(s))ds
\right)
\in \mathbb{R}_+^3
$$

这里的指数是逐通道计算的。

这个式子的物理含义是：光从深度 \(t\) 处的物体表面出发，穿过相机和物体之间的水体到达相机。路径上水体越浑浊，累积衰减越大，最终透射率越小。

## 7. 新的直接光渲染项

原版 3DGS 的直接颜色贡献为：

$$
J(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
$$

加入 3D medium field 后，不应该先把所有 Gaussian 混成 \(J(r)\) 再乘同一个 \(T_m(D)\)，而应该让每个 Gaussian 在自己的深度 \(t_i\) 上受到水体透射率影响：

$$
I_{\mathrm{direct}}(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
\odot
T_m(t_i)
$$

其中：

- \(I_{\mathrm{direct}}(r)\)：穿过水体后到达相机的直接光颜色。
- \(\odot\)：RGB 逐通道乘法。
- \(T_m(t_i)\)：从第 \(i\) 个 scene Gaussian 到相机之间的水体透射率。

如果使用标量 \(\beta\)，则 \(T_m(t_i)\) 是标量，会同时作用于 RGB 三个通道。如果使用 RGB 三通道 \(\boldsymbol{\beta}\)，则 \(T_m(t_i)\) 是 RGB 向量，可以表达红光、绿光、蓝光不同程度的衰减。

这个改动保留了 3DGS 的 alpha compositing 主结构，只是在每个 Gaussian 的颜色贡献上额外乘上 medium transmittance。

## 8. Backscatter 积分项

水下图像中的 backscatter 来自水体本身把环境光散射进相机。它不是场景表面反射光，而是沿 ray 每一小段水体都可能产生的散射光。

如果先不考虑 scene Gaussian 的遮挡，一个基础写法是：

$$
I_{\mathrm{back}}(r)
=
\int_0^D
T_m(t)
\sigma_b(r(t))
b(r(t))
dt
$$

其中：

- \(I_{\mathrm{back}}(r)\)：这条 ray 上累积的 backscatter 颜色。
- \(D\)：ray 的积分终点。若使用 \(T_{gs}(t)\) 显式建模 scene visibility，\(D\) 可以取场景边界或 far plane；若省略 \(T_{gs}(t)\)，\(D\) 应取当前 ray 的有效前景终止深度，避免把物体后方水体的散射加到前景像素。
- \(T_m(t)\)：从 \(r(t)\) 处到相机之间的 medium transmittance。
- \(\sigma_b(r(t))\)：位置 \(r(t)\) 处的 backscatter 强度或散射密度。
- \(b(r(t))\)：位置 \(r(t)\) 处的 backscatter 颜色，通常偏蓝绿，也可以设为全局可学习颜色或 3D 空间颜色场。

但在 3DGS 场景里，ray 上某一段水体产生的 backscatter 也应该受到前景 scene Gaussians 的遮挡。否则物体后方的水体散射会被无条件加到当前像素里，导致亮雾穿过前景物体。

因此更严谨的写法需要加入 scene visibility：

$$
T_{gs}(t)
=
\prod_{j:t_j<t}
\left(1-\alpha_j(r)\right)
$$

其中 \(T_{gs}(t)\) 表示 ray 从相机到位置 \(r(t)\) 之前没有被 scene Gaussians 遮挡的剩余可见性。

于是 backscatter 项应写为：

$$
I_{\mathrm{back}}(r)
=
\int_0^D
T_{gs}(t)
T_m(t)
\sigma_b(r(t))
b(r(t))
dt
$$

如果做最小可行版本，可以先把 \(b\) 设为全局可学习 RGB：

$$
b(r(t)) = b_\infty
$$

然后只学习 \(\sigma_b(x)\) 或直接让 \(\sigma_b(x)\) 和 \(\beta(x)\) 共享同一个 medium density：

$$
\sigma_b(x)
=
\lambda_b \beta_{\mathrm{ext}}(x)
$$

其中 \(\lambda_b\) 是可学习或手动设置的比例系数，通常应限制在非负范围内。如果希望保持物理约束，可以进一步令 \(0\leq\lambda_b\leq 1\)。

## 9. 完整的新渲染公式

完整水下渲染公式为：

$$
I(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
\odot
T_m(t_i)
+
\int_0^D
T_{gs}(t)
T_m(t)
\sigma_b(r(t))
b(r(t))
dt
$$

其中：

$$
T_m(t)
=
\exp
\left(
-\int_0^t
\beta(r(s))ds
\right)
$$

这个公式可以拆成两部分：

直接光项：

$$
I_{\mathrm{direct}}(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
\odot
T_m(t_i)
$$

后向散射项：

$$
I_{\mathrm{back}}(r)
=
\int_0^D
T_{gs}(t)
T_m(t)
\sigma_b(r(t))
b(r(t))
dt
$$

最终图像：

$$
I(r)
=
I_{\mathrm{direct}}(r)
+
I_{\mathrm{back}}(r)
$$

## 10. 每个变量的完整说明

### Ray 和像素变量

- \(r\)：当前像素对应的相机 ray。
- \(r(t)\)：ray 上距离相机 \(t\) 的 3D 点。
- \(o\)：相机中心。
- \(d\)：ray 方向。
- \(t\)：沿 ray 的距离参数。
- \(D\)：积分终点。显式使用 \(T_{gs}(t)\) 时可取场景边界或 far plane；省略 \(T_{gs}(t)\) 时应取当前 ray 的有效前景终止深度。
- \(I(r)\)：当前 ray 对应的最终水下像素颜色。

### Scene Gaussian 变量

- \(i\)：第 \(i\) 个 scene Gaussian。
- \(c_i\)：第 \(i\) 个 scene Gaussian 的颜色。
- \(\alpha_i(r)\)：第 \(i\) 个 scene Gaussian 对当前 ray 的有效 opacity。
- \(T_i^{gs}(r)\)：3DGS alpha compositing 中第 \(i\) 个 Gaussian 前方的透射率。
- \(t_i\)：第 \(i\) 个 scene Gaussian 在当前相机视角下的有效深度。

### Medium field 变量

- \(\beta(x)\)：3D 位置 \(x\) 处的水体消光密度。
- \(\boldsymbol{\beta}(x)\)：RGB 三通道水体消光密度。
- \(G_k(x)\)：第 \(k\) 个 medium Gaussian 在位置 \(x\) 的响应。
- \(m_k\)：第 \(k\) 个 medium Gaussian 的中心。
- \(\Sigma_k\)：第 \(k\) 个 medium Gaussian 的协方差。
- \(w_k\)：第 \(k\) 个 medium Gaussian 的强度。
- \(\boldsymbol{w}_k\)：第 \(k\) 个 medium Gaussian 的 RGB 三通道强度。
- \(T_m(t)\)：从 \(r(t)\) 到相机之间的水体透射率。
- \(T_{gs}(t)\)：从相机到 \(r(t)\) 之前没有被 scene Gaussians 遮挡的剩余可见性。

### Backscatter 变量

- \(\sigma_b(x)\)：3D 位置 \(x\) 处的 backscatter 强度。
- \(b(x)\)：3D 位置 \(x\) 处的 backscatter 颜色。
- \(b_\infty\)：全局远处水体颜色，可作为 \(b(x)\) 的简化形式。
- \(I_{\mathrm{back}}(r)\)：当前 ray 上累积的后向散射颜色。

## 11. 和 SeaSplat 的关键区别

SeaSplat 的形式为：

$$
I(r)
=
J(r)\cdot A(Z(r)) + B(Z(r))
$$

其中：

$$
J(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
$$

SeaSplat 是先把 scene Gaussian 混合成一个 \(J(r)\)，再用最终深度 \(Z(r)\) 对整个像素施加水体影响。

新的公式是：

$$
I(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
\odot
T_m(t_i)
+
\int_0^D
T_{gs}(t)
T_m(t)
\sigma_b(r(t))
b(r(t))
dt
$$

区别在于：

- SeaSplat 的水体影响主要是 depth-only。
- 新公式的水体影响来自 3D 空间场 \(\beta(x)\)。
- SeaSplat 对同一像素内所有 Gaussian 共享一个 \(A(Z)\)。
- 新公式让不同深度的 Gaussian 使用不同的 \(T_m(t_i)\)。
- SeaSplat 的 backscatter 是深度函数。
- 新公式的 backscatter 是沿 ray 对 3D 水体分布积分得到的。

## 12. 为什么要这么做

### 12.1 表达空间非均匀水体

真实水下场景中，浑浊度、悬浮颗粒密度和光照条件可能在 3D 空间中变化。只用最终深度 \(Z\) 很难表达这种空间不均匀性。

通过 3D medium field：

$$
\beta(x)
$$

模型可以表达某些空间区域更浑浊、某些空间区域更清澈。

### 12.2 更符合 ray-level 物理过程

水体影响不是只发生在物体表面，而是发生在相机到物体之间的整条路径上。因此 attenuation 应该由路径积分决定：

$$
T_m(t)
=
\exp
\left(
-\int_0^t
\beta(r(s))ds
\right)
$$

而不是只由最终深度决定。

### 12.3 避免给每个 scene Gaussian 添加自由水体参数

如果直接给每个 scene Gaussian 加 \(\beta_i\)、\(B_i\) 等水体参数，参数量会迅速增加，也容易让物体颜色和水体颜色互相混淆。

低容量 medium field 的好处是：

- 水体参数集中在独立模块里。
- scene Gaussian 仍然负责几何和表面颜色。
- medium field 负责空间介质。
- 参数量可控。
- 物理含义更清晰。

### 12.4 让不同深度的 Gaussian 受到不同水体衰减

3DGS 的一个像素可能由多个深度层的 Gaussian 混合得到。SeaSplat 用一个 \(A(Z)\) 作用于整体 \(J\)，会忽略不同 Gaussian 的深度差异。

新公式中，每个 Gaussian 的贡献使用自己的 medium transmittance：

$$
T_m(t_i)
$$

这更符合多层 alpha compositing 的结构。

## 13. 训练目标

训练时没有真实的 \(\beta(x)\)、\(\sigma_b(x)\)、\(b(x)\) 标注，只有真实水下图像 \(I_{gt}(r)\)。因此可以通过最终图像监督来联合优化 scene Gaussians 和 medium field：

$$
\mathcal{L}_{rgb}
=
\left\|
I(r)-I_{gt}(r)
\right\|_1
+
\lambda_{ssim}
\left(
1-\mathrm{SSIM}(I(r), I_{gt}(r))
\right)
$$

其中：

- \(I(r)\)：新渲染公式输出的水下图像。
- \(I_{gt}(r)\)：真实水下图像。
- \(\lambda_{ssim}\)：SSIM loss 的权重。

为了防止分解不稳定，需要给 medium field 加约束。

### 13.1 Medium 稀疏或低容量约束

限制 \(\beta(x)\) 不要随意解释所有颜色误差：

$$
\mathcal{L}_{density}
=
\int_\Omega
\left\|
\beta(x)
\right\|_1
dx
$$

其中 \(\Omega\) 是场景空间范围。

### 13.2 Medium 平滑约束

真实水体通常不会在很小尺度上剧烈变化，可以加入空间平滑：

$$
\mathcal{L}_{smooth}
=
\int_\Omega
\left\|
\nabla \beta(x)
\right\|_2^2
dx
$$

如果使用 medium Gaussians，也可以通过限制 Gaussian 数量、尺度范围和强度范围来实现低容量约束。

### 13.3 颜色范围约束

对 backscatter 颜色 \(b(x)\) 或 \(b_\infty\) 做范围约束：

$$
0 \leq b(x) \leq 1
$$

这样可以避免 backscatter 学出不合理颜色。

### 13.4 Medium 可辨识性约束

只用水下 RGB 图像监督时，scene Gaussian 颜色 \(c_i\)、直接光衰减 \(\boldsymbol{\beta}(x)\)、backscatter 强度 \(\sigma_b(x)\) 和 backscatter 颜色 \(b(x)\) 之间存在可辨识性问题。模型可能通过调亮物体颜色、同时增大衰减和散射，得到相近的最终图像。

因此建议把 \(\beta\) 明确解释为 extinction density，并限制 backscatter 强度不能任意超过 extinction：

$$
0
\leq
\sigma_b(x)
\leq
\beta_{\mathrm{ext}}(x)
$$

或者使用比例参数化：

$$
\sigma_b(x)
=
\rho_b(x)\beta_{\mathrm{ext}}(x),
\quad
0
\leq
\rho_b(x)
\leq
1
$$

其中：

- \(\beta_{\mathrm{ext}}(x)\)：总消光密度，包含吸收和 out-scattering。
- \(\sigma_b(x)\)：产生 backscatter 的有效散射密度。
- \(\rho_b(x)\)：backscatter 占总消光的比例。

如果使用 RGB 三通道形式，可以把约束逐通道应用到 \(\boldsymbol{\beta}_{\mathrm{ext}}(x)\) 和 \(\boldsymbol{\sigma}_b(x)\) 上。

对应的 soft penalty 可以写成：

$$
\mathcal{L}_{id}
=
\int_\Omega
\left\|
\max
\left(
0,
\sigma_b(x)-\beta_{\mathrm{ext}}(x)
\right)
\right\|_1
dx
$$

如果直接使用 \(\sigma_b(x)=\rho_b(x)\beta_{\mathrm{ext}}(x)\) 且通过 sigmoid 保证 \(0\leq\rho_b(x)\leq1\)，则这个约束可以通过参数化自动满足，不一定需要显式加入 \(\mathcal{L}_{id}\)。

总 loss 可以写成：

$$
\mathcal{L}
=
\mathcal{L}_{rgb}
+
\lambda_{density}\mathcal{L}_{density}
+
\lambda_{smooth}\mathcal{L}_{smooth}
+
\lambda_{id}\mathcal{L}_{id}
+
\lambda_{reg}\mathcal{L}_{reg}
$$

其中：

- \(\lambda_{density}\)：medium density 稀疏约束权重。
- \(\lambda_{smooth}\)：medium field 平滑约束权重。
- \(\lambda_{id}\)：medium 可辨识性约束权重。
- \(\lambda_{reg}\)：其它正则项权重。
- \(\mathcal{L}_{reg}\)：可包含颜色范围、medium Gaussian 尺度范围、强度范围等正则。

## 14. 最小可行实现建议

第一版不要同时做所有内容。建议从最小可行版本开始：

1. 保留原 3DGS scene Gaussian 表示。
2. 新增少量 medium Gaussians 表示 \(\boldsymbol{\beta}(x)\)。
3. 先只建模 path-dependent attenuation，不引入复杂 backscatter 空间场。
4. backscatter 使用全局颜色 \(b_\infty\) 和有效前景深度 \(D_{\mathrm{vis}}\) 的简化近似。
5. 用 ray marching 近似计算 \(T_m(t_i)\)。
6. 将每个 scene Gaussian 的颜色贡献乘以 \(T_m(t_i)\)。
7. 后续再加入带 \(T_{gs}(t)\) 的完整 backscatter ray integral。

第一阶段公式可以简化为：

$$
I(r)
=
\sum_i
T_i^{gs}(r)\alpha_i(r)c_i
\odot
T_m(t_i)
+
b_\infty
\odot
\left(
1-T_m(D_{\mathrm{vis}})
\right)
$$

其中：

$$
T_m(t_i)
=
\exp
\left(
-\int_0^{t_i}
\boldsymbol{\beta}(r(s))ds
\right)
$$

\(D_{\mathrm{vis}}\) 可以取当前 ray 的有效前景深度，例如：

$$
D_{\mathrm{vis}}
\approx
\frac{
\sum_i T_i^{gs}(r)\alpha_i(r)t_i
}{
\sum_i T_i^{gs}(r)\alpha_i(r)+\epsilon
}
$$

这个简化的 backscatter 近似成立的隐含条件是：

$$
\sigma_b(x)
\approx
\beta_{\mathrm{ext}}(x),
\quad
b(x)
\approx
b_\infty
$$

此时：

$$
\int_0^{D_{\mathrm{vis}}}
T_m(t)
\beta_{\mathrm{ext}}(r(t))
dt
=
1-T_m(D_{\mathrm{vis}})
$$

这个版本已经比 SeaSplat 多了一个关键能力：attenuation 不再只是最终深度函数，而是由 ray 穿过的 3D medium field 决定。

如果算力允许，下一步再升级为显式 visibility-aware backscatter：

$$
I_{\mathrm{back}}(r)
=
\int_0^D
T_{gs}(t)
T_m(t)
\sigma_b(r(t))
b_\infty
dt
$$

此时 \(D\) 可以取场景边界或 far plane，因为 \(T_{gs}(t)\) 会降低前景物体之后水体散射对当前像素的贡献。

## 15. 预期贡献表述

可以将方法贡献概括为：

1. 提出一种低容量 3D medium field，用于表示水下场景中的空间非均匀介质。
2. 将 3DGS 的 alpha compositing 与沿 ray 的水体透射率积分结合，使不同深度的 Gaussian 贡献受到不同介质衰减。
3. 将 backscatter 从简单深度函数推广为沿 ray 的空间积分项。
4. 在不为每个 scene Gaussian 添加自由水体参数的前提下，提高水下成像模型的物理一致性和表达能力。

一句话版本：

**我们不是给每个物体 Gaussian 学独立水体参数，而是在场景中额外学习一个低容量 3D medium field，并在 3DGS 渲染过程中沿 camera ray 对水体 attenuation 和 backscatter 进行积分。**
