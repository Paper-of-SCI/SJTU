# patch_guided 当前实现复现说明

本文档记录当前仓库里 `patch_guided` 在真实 3DGS 训练路径中的实现方式，目标是后续可以按同一套代码边界、参数、命令复现实验。

当前实现不是 toy 实验结论，也不是 `rgb_plus_feature` 或 feature loss。真实对比路径只改 3DGS 的 densification 逻辑：在原版屏幕空间梯度的基础上，用当前视角的 `patch_error + patch_edge` 给 Gaussian 的致密化分数加权。

## 1. 实现范围

### Owner

- `modules/densification.py`
  - 负责原版 densification 和 `patch_guided` densification 的算法。
  - 负责 Gaussian clone / split / prune 后同步更新 optimizer state。
- `methods/3dgs/train_3dgs_scene.py`
  - 负责训练主循环。
  - 只根据 `--densification-mode` 选择 densifier，不直接写 patch scoring 细节。
- `modules/gaussian_model.py`
  - 负责 Gaussian 参数容器和基础结构变更。
  - 额外支持 clone 时对子 Gaussian 的位置加 jitter。
- `methods/3dgs/benchmark_patch_densification.py`
  - 负责批量跑训练、测试渲染、汇总指标。

### Responsibility

`patch_guided` 的责任只有一个：改变 clone / split 的选择优先级，让高误差且 GT 边缘强的 image patch 对应的 Gaussian 更容易被 densify。

它不负责：

- 改 photometric loss；
- 加 feature loss；
- 加 semantic mask；
- 改 renderer；
- 改 optimizer 超参；
- 改 dataset split；
- 改 test render 指标计算。

### Dependency Direction

依赖方向保持为：

```text
methods/3dgs/train_3dgs_scene.py
  -> modules.densification.PatchGuidedDensificationController
  -> modules.gaussian_model.GaussianModel
  -> optimizer state patch helpers
```

`modules/densification.py` 不读取文件、不解析命令行、不访问数据集路径。训练脚本把 `render_output`、`optimizer`、`step`、`gt_image` 明确传入。

## 2. 与原版 3DGS baseline 的关系

原版 baseline 使用：

```python
DensificationController(DensificationConfig(...))
```

`patch_guided` 使用：

```python
PatchGuidedDensificationController(
    PatchGuidedDensificationConfig(
        densification=densify_config,
        patch_size=args.patch_size,
        edge_weight=args.patch_edge_weight,
        detail_lambda=args.patch_detail_lambda,
        reallocate_fraction=0.0,
        clone_jitter_scale=args.clone_jitter_scale,
    )
)
```

除了 densifier 不同，训练脚本保持同数据、同 split、同 loss、同 optimizer、同 seed、同 iterations。

当前真实对比里 `patch_guided` 没有启用 reallocation：

```python
reallocate_fraction=args.reallocate_fraction if args.densification_mode == "patch_reallocate" else 0.0
```

所以：

- `standard_3dgs`：原版梯度 densification。
- `patch_guided`：`patch_error + patch_edge` 调整梯度分数，只影响 clone / split 优先级。
- `patch_reallocate`：在 `patch_guided` 基础上再额外 prune 低 detail Gaussian，并把预算转给高分 Gaussian。这个不是当前最终对比主线。

## 3. 关键配置接口

### `DensificationConfig`

文件位置：`modules/densification.py`

```python
@dataclass
class DensificationConfig:
    start_step: int = 500
    stop_step: int = 15_000
    interval: int = 100
    grad_threshold: float = 2.0e-4
    scene_extent: float = 1.0
    percent_dense: float = 0.01
    min_opacity: float = 0.005
    max_screen_radius: Optional[float] = None
    num_splits: int = 2
    opacity_reset_interval: int = 3000
    reset_opacity: float = 0.01
    use_absgrad: bool = True
```

训练脚本里的实际默认值有一处覆盖：

```python
--densify-grad-threshold default=2.0e-5
```

因此真实 3DGS benchmark 默认用的是：

```python
DensificationConfig(
    start_step=500,
    stop_step=max(args.iterations - 500, 501),
    interval=100,
    grad_threshold=args.densify_grad_threshold,  # 默认 2.0e-5
    scene_extent=float(scene.scene_extent),
    percent_dense=0.01,
    min_opacity=0.005,
    opacity_reset_interval=3000,
)
```

含义：

- `start_step=500`：前 500 步只训练，不 clone / split / prune。
- `stop_step=max(iterations - 500, 501)`：最后 500 步停止 densification，只微调已有 Gaussian。
- `interval=100`：每 100 步执行一次 densification。
- `grad_threshold`：判定高梯度 Gaussian 的阈值。
- `percent_dense=0.01`：小 Gaussian clone，大 Gaussian split 的分界比例。
- `min_opacity=0.005`：低 opacity Gaussian 会被 prune。
- `opacity_reset_interval=3000`：每 3000 步重置 opacity，并清空对应 optimizer state。

### `PatchGuidedDensificationConfig`

文件位置：`modules/densification.py`

```python
@dataclass
class PatchGuidedDensificationConfig:
    densification: DensificationConfig = field(default_factory=DensificationConfig)
    patch_size: int = 16
    edge_weight: float = 0.75
    detail_lambda: float = 2.0
    reallocate_fraction: float = 0.0
    clone_jitter_scale: float = 0.05
    eps: float = 1.0e-6
```

真实 `patch_guided` 默认值：

- `patch_size=16`
- `edge_weight=0.75`
- `detail_lambda=2.0`
- `reallocate_fraction=0.0`
- `clone_jitter_scale=0.05`
- `eps=1.0e-6`

命令行参数来自 `methods/3dgs/train_3dgs_scene.py`：

```bash
--patch-size 16
--patch-edge-weight 0.75
--patch-detail-lambda 2.0
--clone-jitter-scale 0.05
```

`patch_guided` 的核心加权公式是：

```python
adjusted_grad = avg_grad * (1.0 + detail_lambda * avg_detail)
high_grad = adjusted_grad >= grad_threshold
```

其中：

- `avg_grad` 是原版 3DGS 累计得到的屏幕空间梯度均值；
- `avg_detail` 是每个 Gaussian 在最近 densification interval 内投影到 image patch 后累计的 patch detail 均值；
- `detail_lambda` 控制 patch detail 对梯度分数的放大强度。

## 4. 训练主循环里的调用时机

文件位置：`methods/3dgs/train_3dgs_scene.py`

每一步训练流程如下：

```text
随机取一张训练相机
  -> 读取该相机 GT 图像
  -> renderer.render(model, camera)
  -> photometric_loss(render.image, gt_image)
  -> loss.backward()
  -> optimizer.step()
  -> densifier.update(...)
```

代码中关键调用是：

```python
render = renderer.render(model, camera)
loss, parts = photometric_loss(render.image.clamp(0.0, 1.0), gt_image, lambda_dssim=args.lambda_dssim)
loss.backward()
optimizer.step()

if uses_patch_densifier:
    stats = densifier.update(model, render, optimizer, step, gt_image)
else:
    stats = densifier.update(model, render, optimizer, step)
```

注意这里当前实现是在 `optimizer.step()` 之后调用 densifier。`render_output.means2d` 上仍保留本轮 backward 得到的 screen-space gradient，densifier 用它累计梯度统计。

`patch_guided` 比 baseline 多传入一个窄输入：

```python
gt_image: Tensor
```

这是为了在 densifier 内部计算当前视角的 `patch_error + patch_edge`。训练脚本不暴露 patch detail 的内部结构。

## 5. `PatchGuidedDensificationController.update`

文件位置：`modules/densification.py`

接口：

```python
def update(
    self,
    model: GaussianModel,
    render_output: RenderOutput,
    optimizer: torch.optim.Optimizer,
    step: int,
    gt_image: Tensor,
) -> DensificationStats:
```

输入：

- `model`：当前 `GaussianModel`。
- `render_output`：当前视角渲染结果，至少使用：
  - `render_output.image`
  - `render_output.means2d`
  - `render_output.radii`
  - `render_output.metadata.get("gaussian_ids")`
- `optimizer`：当前 Adam optimizer，用于同步参数长度和 optimizer state。
- `step`：当前训练步数。
- `gt_image`：当前训练视角 GT，shape 为 `CHW`。

输出：

```python
DensificationStats(
    cloned=...,
    split=...,
    pruned=...,
    total=...,
    densified=...,
    high_grad=...,
    grad_mean=...,
    grad_max=...,
    reallocated=...,
    patch_detail_mean=...,
    patch_detail_max=...,
)
```

错误语义：

- 如果 `render_image` 和 `gt_image` shape 不一致，`_compute_patch_detail` 会抛出：

```text
render_image and gt_image must have the same CHW shape
```

- 如果输入不是 `CHW` 3 维 tensor，会抛出：

```text
render_image and gt_image must be CHW tensors
```

边界条件：

- `means2d is None` 时不会累计 patch detail。
- 没有可见投影时，detail 累计为 0。
- 投影在图像边界外、坐标非有限、`radii <= 0` 的 Gaussian 不累计 detail。
- `model.num_gaussians == 0` 时直接返回空 stats。

## 6. 每步累计的两类信号

### 6.1 原版 screen-space gradient

`patch_guided` 仍然复用原版梯度累计：

```python
model.accumulate_gradient_stats(
    render_output.means2d,
    visibility=visibility,
    use_absgrad=base.use_absgrad,
    indices=render_output.metadata.get("gaussian_ids"),
)
```

这里：

- `visibility` 来自 `_visibility_from_output(render_output, model.num_gaussians)`；
- 如果 `radii` 长度等于 Gaussian 数量，则 `visibility = radii > 0`；
- `use_absgrad=True` 是默认值；
- `gaussian_ids` 用于处理 renderer 输出不是一一对应 Gaussian 顺序的情况。

每个 Gaussian 内部会累计：

```python
gradient_accum
gradient_count
```

densify 时使用：

```python
avg_grad = model.gradient_accum / model.gradient_count.clamp_min(1.0)
```

### 6.2 patch detail

`patch_guided` 在每步额外计算当前训练视角的 patch detail：

```python
detail_map = _compute_patch_detail(
    render_output.image.detach(),
    gt_image.detach(),
    patch_size=self.config.patch_size,
    edge_weight=self.config.edge_weight,
    eps=self.config.eps,
)
```

然后把每个 Gaussian 的 2D 投影映射到 patch grid：

```python
detail_values, detail_counts = _project_patch_detail_to_gaussians(
    detail_map,
    render_output.means2d.detach(),
    render_output.metadata.get("gaussian_ids"),
    render_output.radii,
    gaussian_count=model.num_gaussians,
    image_height=int(gt_image.shape[-2]),
    image_width=int(gt_image.shape[-1]),
    patch_size=self.config.patch_size,
)
```

再累计到 controller 内部 buffer：

```python
self._detail_accum.add_(detail_values)
self._detail_count.add_(detail_counts)
```

这两个 buffer 的长度始终等于当前 Gaussian 数量：

```python
self._detail_accum: Optional[Tensor]
self._detail_count: Optional[Tensor]
```

每次 densification 后会清空：

```python
self._clear_buffers(model)
```

所以 `avg_detail` 只代表上一个 densification interval 内的 patch detail 均值，不跨整个训练永久累计。

## 7. patch detail 的计算细节

函数位置：`modules/densification.py::_compute_patch_detail`

接口：

```python
def _compute_patch_detail(
    render_image: Tensor,
    gt_image: Tensor,
    patch_size: int,
    edge_weight: float,
    eps: float = 1.0e-6,
) -> Tensor:
```

输入 shape：

```text
render_image: (3, H, W)
gt_image:     (3, H, W)
```

输出 shape：

```text
detail_map: (ceil(H / patch_size), ceil(W / patch_size))
```

当前默认 `patch_size=16`。例如测试分辨率 `444x295` 时，patch grid 大约是：

```text
ceil(295 / 16) x ceil(444 / 16) = 19 x 28
```

### 7.1 pixel error

先把渲染图和 GT clamp 到 `[0, 1]`：

```python
render = render_image.detach().clamp(0.0, 1.0)
gt = gt_image.detach().clamp(0.0, 1.0)
```

逐像素误差：

```python
pixel_error = (render - gt).abs().mean(dim=0, keepdim=True).unsqueeze(0)
```

含义：

- RGB 三通道取平均 L1；
- shape 从 `(3, H, W)` 变成 `(1, 1, H, W)`。

### 7.2 patch error

用 `avg_pool2d` 做 patch pooling：

```python
patch_error = F.avg_pool2d(
    pixel_error,
    kernel_size=patch_size,
    stride=patch_size,
    ceil_mode=True,
    count_include_pad=False,
)
```

关键点：

- `ceil_mode=True`：图像右边和底边不足一个 patch 的区域也会保留。
- `count_include_pad=False`：边界 patch 不会因为 padding 被平均稀释。

### 7.3 patch edge

边缘只从 GT 图像计算，不从 render 计算：

```python
gray = gt.mean(dim=0, keepdim=True).unsqueeze(0)
```

使用 Sobel kernel：

```python
kernel_x = [[1, 0, -1], [2, 0, -2], [1, 0, -1]]
kernel_y = [[1, 2, 1], [0, 0, 0], [-1, -2, -1]]
```

边界 padding：

```python
padded = F.pad(gray, (1, 1, 1, 1), mode="replicate")
```

边缘强度：

```python
edge = sqrt(grad_x ** 2 + grad_y ** 2)
```

然后同样做 patch pooling：

```python
patch_edge = _patch_pool(_sobel_edge(gt), patch)
```

### 7.4 min-max normalization

`patch_error` 和 `patch_edge` 分别归一化到 `[0, 1]`：

```python
patch_error = _normalize_minmax(patch_error, eps)
patch_edge = _normalize_minmax(patch_edge, eps)
```

归一化逻辑：

```python
normalized = (values - min_value) / max(max_value - min_value, eps)
```

如果整张图所有 patch 值几乎相同，直接返回全 0，避免除 0：

```python
torch.where(denom > eps, normalized, torch.zeros_like(values))
```

### 7.5 detail fusion

当前融合公式：

```python
detail = patch_error * (1.0 + edge_weight * patch_edge)
detail = _normalize_minmax(detail)
```

默认 `edge_weight=0.75`，所以边缘强的 patch 会放大 error 的重要性，但如果 error 本身很低，detail 也不会凭空变高。

语义上：

- 高 `patch_error`：当前模型在这个区域重建不好；
- 高 `patch_edge`：GT 这个区域结构或纹理边界明显；
- 高 `detail`：这个区域更值得分配 Gaussian 容量。

## 8. Gaussian 到 patch 的投影累计

函数位置：`modules/densification.py::_project_patch_detail_to_gaussians`

接口：

```python
def _project_patch_detail_to_gaussians(
    detail_map: Tensor,
    means2d: Tensor,
    gaussian_ids: Optional[Tensor],
    radii: Optional[Tensor],
    gaussian_count: int,
    image_height: int,
    image_width: int,
    patch_size: int,
) -> tuple[Tensor, Tensor]:
```

输出：

- `values`: shape `(gaussian_count,)`
- `counts`: shape `(gaussian_count,)`

含义：

- `values[i]`：当前视角里第 `i` 个 Gaussian 投影所在 patch 的 detail 累计值。
- `counts[i]`：当前视角里第 `i` 个 Gaussian 有效投影次数。

### 8.1 flatten projection

先把 `means2d` flatten：

```python
xy = means2d.reshape(-1, means2d.shape[-1])[:, :2]
```

Gaussian id 的来源有三种情况：

1. 如果 `render_output.metadata["gaussian_ids"]` 存在，用它作为 Gaussian index。
2. 如果 `xy.shape[0] == gaussian_count`，认为投影顺序和 Gaussian 顺序一致。
3. 如果 `xy.shape[0]` 是 `gaussian_count` 的整数倍，认为每个 Gaussian 有重复投影，按顺序 repeat index。

否则返回空累计。

### 8.2 validity mask

有效投影必须满足：

```python
0 <= index < gaussian_count
isfinite(xy)
0 <= x < image_width
0 <= y < image_height
radii > 0  # 如果 radii 可用
```

无效投影不会给 Gaussian 累计 detail。

### 8.3 patch index

把像素坐标转 patch 坐标：

```python
px = floor(x) // patch_size
py = floor(y) // patch_size
```

并 clamp 到 detail map 范围内：

```python
px = px.clamp(0, patch_w - 1)
py = py.clamp(0, patch_h - 1)
```

取 patch detail：

```python
patch_values = detail_map.reshape(-1)[py * patch_w + px]
```

最后用 `index_add_` 累加到每个 Gaussian：

```python
values.index_add_(0, valid_indices, patch_values[valid])
counts.index_add_(0, valid_indices, ones)
```

## 9. densification 决策逻辑

函数位置：`modules/densification.py::PatchGuidedDensificationController._densify`

### 9.1 计算原版梯度均值

```python
avg_grad = model.gradient_accum / model.gradient_count.clamp_min(1.0)
```

### 9.2 计算 Gaussian detail 均值

```python
avg_detail = self._detail_accum / self._detail_count.clamp_min(1.0)
```

如果某个 Gaussian 在 interval 内没有有效投影，则：

```python
detail_count = 0
avg_detail = 0
```

它退化成原版梯度分数。

### 9.3 调整梯度分数

核心公式：

```python
adjusted_grad = avg_grad * (1.0 + detail_lambda * avg_detail)
```

默认 `detail_lambda=2.0`。由于 `avg_detail` 在 `[0, 1]`，所以理论上梯度最多被放大到：

```text
avg_grad * 3.0
```

然后用同一个 threshold：

```python
high_grad = adjusted_grad >= base.grad_threshold
```

这保证 `patch_guided` 没有新增独立 threshold，只是复用原版 `grad_threshold`。

### 9.4 clone / split 分界

和原版 3DGS 保持一致：

```python
max_scale = model.scales.detach().max(dim=-1).values
dense_threshold = scene_extent * percent_dense
```

小 Gaussian clone：

```python
clone_mask = high_grad & (max_scale <= dense_threshold) & ~prune_mask
```

大 Gaussian split：

```python
split_mask = high_grad & (max_scale > dense_threshold) & ~prune_mask
```

也就是说，patch detail 不直接决定 clone 还是 split，只决定更容易进入 `high_grad`。

### 9.5 prune

`patch_guided` 的普通 prune 仍然是原版逻辑：

```python
prune_mask = model.opacities.detach() < min_opacity
```

如果配置了 `max_screen_radius`，还会 prune 屏幕半径过大的 Gaussian：

```python
prune_mask = prune_mask | (radii > max_screen_radius)
```

当前训练脚本没有设置 `max_screen_radius`，所以主要 prune 低 opacity。

### 9.6 应用结构变化

当前 `patch_guided` 不是先调用 `model.clone()` 再 `model.split()` 再 `model.prune()`，而是一次性构造子 Gaussian，然后一次性 append + prune。

原因是 clone / split / prune 同时发生时，需要保持 optimizer state 和 Gaussian 参数长度一致。

流程：

```python
old = _snapshot_optimizer(model, optimizer)
clone_tensors = _build_clone_tensors(model, clone_mask, clone_jitter_scale)
split_tensors = _build_split_tensors(model, split_mask, num_splits)
child_tensors = _concat_child_tensors(model, [clone_tensors, split_tensors])
keep_old = ~(split_mask | prune_mask)
_apply_append_and_prune_tensors(model, optimizer, old, keep_old, child_tensors)
```

保留旧 Gaussian：

```python
keep_old = ~(split_mask | prune_mask)
```

含义：

- clone 的 parent 保留；
- split 的 parent 删除；
- prune 的 Gaussian 删除；
- clone child 和 split child append 到末尾。

### 9.7 清空 interval 统计

densify 结束后清空：

```python
model.clear_gradient_stats()
self._clear_buffers(model)
```

所以下一轮 100 步 interval 从 0 重新累计。

## 10. clone 和 split 的参数复制规则

### 10.1 clone

函数位置：

- `modules/densification.py::_build_clone_tensors`
- `modules/gaussian_model.py::GaussianModel.clone`

`patch_guided` 使用 `_build_clone_tensors`。它会复制所有 Gaussian 参数：

```python
PARAMETER_NAMES = [
    "means",
    "log_scales",
    "quats",
    "logit_opacities",
    "features_dc",
    "features_rest",
]
```

复制规则：

- `log_scales` 完全复制；
- `quats` 完全复制；
- `logit_opacities` 完全复制；
- `features_dc` 完全复制；
- `features_rest` 完全复制；
- `means` 默认复制后加一个局部随机 jitter。

clone jitter：

```python
local_offsets = torch.randn_like(means) * model.scales[selected] * clone_jitter_scale
world_offsets = rotations @ local_offsets
child_means = parent_means + world_offsets
```

默认：

```text
clone_jitter_scale = 0.05
```

即 clone child 的初始位置会按父 Gaussian 当前 scale 的 5% 随机偏移，避免 clone 后完全重合。

### 10.2 split

函数位置：`modules/densification.py::_build_split_tensors`

默认：

```python
num_splits = 2
scale_shrink = 1.6
```

split child 规则：

```python
samples = torch.randn(selected_count, num_splits, 3)
local_offsets = samples * scales
world_offsets = rotations @ local_offsets
child_means = parent_means + world_offsets
```

scale 变小：

```python
child_log_scales = parent_log_scales - log(1.6)
```

其他参数复制：

- `quats` 复制；
- `logit_opacities` 复制；
- `features_dc` 复制；
- `features_rest` 复制。

split parent 会被删除，split child append 到末尾。

## 11. optimizer state 同步

Gaussian 数量变化后，Adam optimizer 的 state 也必须同步变化，否则后续 step 会因为参数长度不一致出错。

相关函数在 `modules/densification.py`：

- `_snapshot_optimizer`
- `_apply_append_and_prune_tensors`
- `_expanded_state`
- `_masked_state`
- `_replace_group_param`
- `_zero_optimizer_state_for`

### 11.1 snapshot

结构变化前保存旧 optimizer state：

```python
old = _snapshot_optimizer(model, optimizer)
```

对每个参数组保存：

```python
{
    "param": param,
    "state": cloned_state,
}
```

### 11.2 append

新增 child Gaussian 时，对 Adam state 追加 0：

```python
pad = torch.zeros((added, *value.shape[1:]), dtype=value.dtype, device=value.device)
out[key] = torch.cat([value, pad], dim=0)
```

含义是新 Gaussian 的 Adam 动量从 0 开始。

### 11.3 prune

删除旧 Gaussian 时，对 state 使用同一个 keep mask：

```python
out[key] = value[keep]
```

### 11.4 替换 optimizer param

`model.replace_tensors(updated)` 会创建新的 `torch.nn.Parameter`。因此 optimizer 里的旧 param 引用必须替换：

```python
group["params"] = [param]
optimizer.state[param] = state
```

这一步是保证：

```text
Gaussian 参数长度 == optimizer state 第一维长度
```

## 12. `patch_guided` 和 `patch_reallocate` 的差异

当前最终对比主线只使用 `patch_guided`。

区别如下：

### patch_guided

```python
reallocate_fraction = 0.0
```

行为：

- 不额外 prune 低 detail Gaussian；
- 只用 patch detail 放大 gradient；
- clone / split 数量由 `adjusted_grad >= grad_threshold` 自然决定。

### patch_reallocate

```python
reallocate_fraction > 0.0
```

行为：

1. 从没有进入 clone / split / prune 的 Gaussian 里选一批低价值 Gaussian 做额外 prune。
2. 用释放出来的预算去 clone / split 高分 Gaussian。

低价值分数：

```python
low_score = (
    (1.0 - detail_norm) * 0.55
    + (1.0 - seen_norm) * 0.20
    + (1.0 - opacity) * 0.25
)
```

高价值分数：

```python
source_score = adjusted_grad * (1.0 + avg_detail)
```

真实实验里 `patch_reallocate` 目前不稳定，所以后续主线先看 `patch_guided`。

## 13. 训练脚本参数

文件位置：`methods/3dgs/train_3dgs_scene.py`

关键参数：

```bash
--densification-mode standard|standard_3dgs|patch_guided|patch_reallocate
--densify-grad-threshold 2.0e-5
--patch-size 16
--patch-edge-weight 0.75
--patch-detail-lambda 2.0
--reallocate-fraction 0.10
--clone-jitter-scale 0.05
```

其中 `patch_guided` 实际不会用 `--reallocate-fraction`，因为训练脚本会把它置为 0：

```python
reallocate_fraction=args.reallocate_fraction if args.densification_mode == "patch_reallocate" else 0.0
```

## 14. benchmark runner

文件位置：`methods/3dgs/benchmark_patch_densification.py`

runner 对每组实验做两件事：

1. 调用 `methods/3dgs/train_3dgs_scene.py` 训练并生成 `final.ply`。
2. 调用 `methods/3dgs/render_3dgs_views.py` 渲染 test split 并生成 `metrics.csv`。

训练命令由 runner 组装，关键参数会传到训练脚本：

```python
train_cmd = [
    sys.executable,
    "methods/3dgs/train_3dgs_scene.py",
    "--data", data_dir,
    "--out", train_dir,
    "--iterations", iterations,
    "--factor", factor,
    "--holdout", holdout,
    "--seed", seed,
    "--densification-mode", variant,
    "--densify-grad-threshold", densify_grad_threshold,
    "--patch-size", patch_size,
    "--patch-edge-weight", patch_edge_weight,
    "--patch-detail-lambda", patch_detail_lambda,
    "--reallocate-fraction", reallocate_fraction,
    "--clone-jitter-scale", clone_jitter_scale,
    "--save-every", iterations,
    "--eval-every", "0",
    "--log-every", log_every,
]
```

测试渲染命令：

```python
render_cmd = [
    sys.executable,
    "methods/3dgs/render_3dgs_views.py",
    "--data", data_dir,
    "--checkpoint", final_ply,
    "--out", render_dir,
    "--split", "test",
    "--factor", factor,
    "--holdout", holdout,
]
```

如果传 `--lpips`，渲染阶段额外计算 LPIPS。

## 15. 输出文件

单个 run 的目录结构：

```text
outputs/.../{scene}/{variant}/seed_000/
  train/
    final.ply
    training_summary.json
    previews/
      step_000001.png
      step_007000.png
    checkpoints/
      step_007000.ply
  test_renders/
    metrics.csv
    000_xxx_render.png
    000_xxx_gt.png
    ...
```

`training_summary.json` 记录：

```json
{
  "data": "...",
  "out": "...",
  "iterations": 7000,
  "factor": 4,
  "holdout": 8,
  "seed": 0,
  "densification_mode": "patch_guided",
  "disable_densification": false,
  "densify_grad_threshold": 2e-05,
  "patch_size": 16,
  "patch_edge_weight": 0.75,
  "patch_detail_lambda": 2.0,
  "reallocate_fraction": 0.1,
  "clone_jitter_scale": 0.05,
  "final_gaussians": 35583,
  "elapsed_seconds": 141.9
}
```

注意：这里 `reallocate_fraction` 会保存命令行输入值，但 `patch_guided` 实际 controller 内部用的是 `0.0`。只有 `patch_reallocate` 才使用该值。

runner 汇总输出：

```text
outputs/.../summary.csv
outputs/.../summary.json
```

字段包括：

```text
scene, variant, seed, iterations, factor, holdout,
psnr, ssim, l1, lpips,
gaussian_count, train_seconds, total_seconds,
data, run_dir, final_ply, metrics_csv
```

## 16. 复现实验命令

### 单元测试

```bash
/home/leo/miniconda3/envs/sfquant/bin/python -m unittest tests.test_3dgs_patch_densification
```

测试覆盖：

- patch detail shape 和归一化；
- Gaussian 投影到 patch 的边界条件；
- clone jitter 与复制规则；
- patch controller 更新后 optimizer state 长度一致；
- reallocation 预算可控和 optimizer state 一致。

### 单场景 smoke

```bash
/home/leo/miniconda3/envs/sfquant/bin/python methods/3dgs/benchmark_patch_densification.py \
  --scenes Curasao \
  --variants standard_3dgs patch_guided \
  --seeds 0 \
  --iterations 300 \
  --factor 8 \
  --holdout 8 \
  --lpips \
  --out outputs/3dgs_patch_smoke
```

### 正式对比

```bash
/home/leo/miniconda3/envs/sfquant/bin/python methods/3dgs/benchmark_patch_densification.py \
  --scenes Curasao IUI3-RedSea JapaneseGradens-RedSea Panama \
  --variants standard_3dgs patch_guided \
  --seeds 0 1 \
  --iterations 7000 \
  --factor 4 \
  --holdout 8 \
  --lpips \
  --out outputs/3dgs_patch_reproduce
```

如果只想跳过已有结果，需要已有 run 同时存在 `final.ply` 和 `metrics.csv`：

```bash
--skip-existing
```

## 17. 当前已经跑过的结果口径

当前最终汇总结果来自：

- `outputs/3dgs_patch_curasao/summary.csv`
  - 使用其中 `Curasao` 的 `seed=0,1`
  - 只取 `standard_3dgs` 和 `patch_guided`
- `outputs/3dgs_patch_other_scenes/summary.csv`
  - 使用 `IUI3-RedSea`、`JapaneseGradens-RedSea`、`Panama`
  - `seed=0,1`
  - 只取 `standard_3dgs` 和 `patch_guided`

最终合并文件：

```text
outputs/3dgs_patch_final_comparison/summary_by_scene.csv
outputs/3dgs_patch_final_comparison/summary_overall.json
```

总体均值：

```text
standard_3dgs:
  PSNR  = 27.459
  SSIM  = 0.8658
  LPIPS = 0.2816
  L1    = 0.02867
  Gaussian count = 32762
  train_seconds  = 112.9

patch_guided:
  PSNR  = 27.444
  SSIM  = 0.8697
  LPIPS = 0.2651
  L1    = 0.02873
  Gaussian count = 53216
  train_seconds  = 146.2
```

差值 `patch_guided - standard_3dgs`：

```text
PSNR  = -0.015
SSIM  = +0.0039
LPIPS = -0.0164
L1    = +0.00005
Gaussian count = +20455
train_seconds  = +33.4s
```

解释：

- `LPIPS` 下降是好事；
- `SSIM` 上升是好事；
- `PSNR` 基本持平但不稳定；
- Gaussian 数量明显增加，说明当前 scoring 偏激进。

## 18. GT 和 render 看起来不清晰的原因

当前实验使用：

```bash
--factor 4
```

因此保存到 `test_renders` 的 GT 不是原始分辨率图，而是 dataloader 降采样后的 GT。

已确认尺寸：

```text
Curasao:
  source GT: 1776x1182
  output GT: 444x295

Panama:
  source GT: 1776x1182
  output GT: 444x295

IUI3-RedSea:
  source GT: 1370x913
  output GT: 342x228

JapaneseGradens-RedSea:
  source GT: 1370x913
  output GT: 342x228
```

所以如果在图片查看器里放大看，连 `*_gt.png` 也会显得模糊或像低分辨率图。

另一个原因是数据本身是水下图像，本来就有：

- 水体散射；
- haze；
- backscatter；
- 低对比度；
- 运动或成像模糊；
- white balance 预处理后的颜色和纹理变软。

如果要做更清晰的视觉复现，应使用：

```bash
--factor 2
```

或者：

```bash
--factor 1
```

但公平对比时，`standard_3dgs` 和 `patch_guided` 必须使用同一个 `factor`、同一个 `holdout`、同一个 `seed`、同一个 `iterations`。

## 19. 当前实现的局限

1. `patch_guided` 没有 semantic mask。
   - 当前真实数据没有接 SAM / CLIP / segmentation。
   - detail 只由 render error 和 GT edge 决定。

2. `patch_detail` 是 per-view、per-interval 统计。
   - 每 100 步 densify 后清空。
   - 不维护跨训练全局 patch memory。

3. `detail_lambda=2.0` 会显著增加 Gaussian 数量。
   - 当前 4 个场景平均 Gaussian 数约增加 62%。
   - 说明需要后续加预算约束或更稳的归一化。

4. `patch_guided` 只改变 densification，不改变 loss。
   - 如果 PSNR 不升，不能靠 feature loss 或 semantic loss 解释。
   - 当前结果主要表现为 SSIM / LPIPS 改善，PSNR 不稳定。

5. 当前 `training_summary.json` 对 `patch_guided` 会记录命令行里的 `reallocate_fraction=0.1`。
   - 这是元数据记录值。
   - 实际 controller 中 `patch_guided` 的 `reallocate_fraction` 是 0。

## 20. 后续如果继续改，建议的最小方向

为了不让 Gaussian 数量失控，优先改 `PatchGuidedDensificationConfig` 或 `_densify` 的 scoring，不要改训练主循环。

推荐方向：

1. 降低 `detail_lambda`
   - 例如从 `2.0` 改到 `0.5` 或 `1.0`。

2. 限制每次 densification 的新增数量
   - 在 `clone_mask` / `split_mask` 后加 top-k budget。

3. 对 `avg_detail` 做 softer normalization
   - 当前每张图 min-max 可能把局部噪声拉满。

4. 把 `patch_edge` 改成更稳的结构信号
   - 例如 blur 后 Sobel 或 multi-scale edge。

5. 修正 `training_summary.json` 中 `patch_guided` 的有效 reallocation 记录
   - 把实际 controller 使用的 `reallocate_fraction` 写进去，避免复现实验误读。

## 21. 最小复现检查清单

复现时至少确认：

- Python 使用 `/home/leo/miniconda3/envs/sfquant/bin/python`。
- CUDA 可用。
- `--variants standard_3dgs patch_guided`。
- 两个 variant 使用同一组：
  - `--scenes`
  - `--seeds`
  - `--iterations`
  - `--factor`
  - `--holdout`
  - `--densify-grad-threshold`
  - `--patch-size`
  - `--patch-edge-weight`
  - `--patch-detail-lambda`
  - `--clone-jitter-scale`
- 每个 run 都生成：
  - `train/final.ply`
  - `train/training_summary.json`
  - `test_renders/metrics.csv`
- 汇总时只比较相同 scene 和相同 seed。
- 看图时记住 `test_renders/*_gt.png` 是按 `factor` 降采样后的 GT。
