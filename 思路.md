# 语义 / Patch 引导的 3DGS Densification 思路

## 1. 核心判断

论文里如果只是给每个 Gaussian primitive 额外加一个语义特征，并对这个语义特征做 loss，但这个语义特征不参与 RGB 渲染、几何更新、opacity 更新或 densification，那么它对最终 NVS 质量的帮助可能比较有限。

更直接的方向是：不要只让语义特征作为一个“附属输出”，而是把语义 / patch 先验用来控制 3DGS 原本的 densification 过程，也就是控制哪些 Gaussian 更应该 clone、split，哪些区域可以更积极 prune 或 reallocate。

目标不是让语义特征直接生成图像，而是让有限的 Gaussian 数量更多分配到高细节、高价值、当前误差大的区域。

## 2. 原版 3DGS 的 densification 逻辑

原版 3DGS 主要根据 Gaussian 在屏幕空间的梯度来决定是否 densify：

```text
如果某个 Gaussian 的 screen-space position gradient 很大，
说明它投影到图像上的区域拟合压力比较大，
就说明这里可能需要更多 Gaussian。
```

一般策略是：

```text
小 Gaussian：clone
大 Gaussian：split
低 opacity / 低贡献 Gaussian：prune
```

clone 的新 Gaussian 初始位置通常等于父 Gaussian 的位置，其他参数也从父 Gaussian 复制过来。这样确实会有完全重合的问题，两个 Gaussian 不一定天然分化，所以如果做语义引导版本，建议不要直接纯 clone，而是使用轻微扰动的 clone，或者优先使用 split。

更合理的新球位置：

```text
mu_child = mu_parent + R_parent @ epsilon
epsilon ~ Normal(0, eta * scale_parent)
```

其中 `eta` 可以先取 `0.05 ~ 0.15`。这样新 Gaussian 仍然在父 Gaussian 附近，但不是完全重合。

对于大的 Gaussian，可以沿用 split：

```text
mu_child = mu_parent + R_parent @ delta
delta ~ Normal(0, scale_parent)
```

然后缩小 child 的 scale。

## 3. 关键问题：哪个 patch 需要更多细节？

不能只靠语义类别判断。比如 coral 是重要目标，但如果当前已经拟合得很好，也不一定继续加球；水体通常不重要，但如果当前误差很大，也不能完全忽略。

建议把 patch 需求分成三类信号：

```text
1. 当前重建误差：rendered image 和 gt image 的差异
2. 图像边缘 / 纹理复杂度：这个区域本身有没有细节
3. 语义重要性：这个区域是不是值得保留和精细重建
```

一个可用的 patch_detail 公式：

```text
patch_detail =
    normalize(patch_error)
    * (1 + alpha * normalize(patch_edge))
    * (base + (1 - base) * semantic_importance)
```

其中：

```text
patch_error：patch 内平均 RGB 误差，比如 |I_render - I_gt|
patch_edge：patch 内 Sobel / Laplacian 边缘强度
semantic_importance：语义重要性，范围 0~1
base：保底权重，比如 0.2，防止语义漏检后完全禁止 densification
alpha：边缘权重，比如 0.5~1.0
```

如果第一版不想引入语义模型，可以先做：

```text
patch_detail = normalize(patch_error) * (1 + alpha * normalize(patch_edge))
```

这能先验证 patch-aware densification 是否有效。

## 4. 语义重要性怎么得到

语义重要性不是 ViT 自己自动给出的“重要 / 不重要”标签，而是需要人为定义或通过模型输出转换。

### 方案 A：segmentation / mask

用 segmentation、SAM、Grounded-SAM、CLIPSeg 等方法得到目标 mask，然后把 mask 平均池化到 patch 网格。

例如对每个像素先有类别概率 `P(c)`，再给每类一个权重 `W_c`：

```text
semantic_importance_p = sum_c P_p(c) * W_c
```

水下场景可以先粗略设：

```text
coral / shipwreck / object：1.0
seabed / rock：0.5 ~ 0.7
water / haze / background：0.05 ~ 0.2
fish / marine snow / dynamic distractor：0 ~ 0.3
```

如果只有二值 mask，可以直接做 patch pooling：

```python
patch_score = avg_pool2d(
    mask.float()[None, None],
    kernel_size=patch_size,
    stride=patch_size,
)[0, 0]
```

### 方案 B：CLIP / ViT patch feature

如果有 ViT patch feature，可以把每个 patch 的 feature 和一些文本 prompt embedding 做相似度：

```text
重要 prompt：coral, shipwreck, object, structure, seabed detail
不重要 prompt：water, haze, background, snow particle, blurry region
```

然后：

```text
semantic_importance =
    softmax_similarity(important prompts)
    - softmax_similarity(unimportant prompts)
```

这种方案更灵活，但不如 segmentation mask 稳定。第一版建议优先用 mask 或者直接用 error + edge。

## 5. patch_detail 怎么分配给 Gaussian

训练时每个 Gaussian 都会投影到当前相机视角的 2D 图像上。可以利用它的投影位置找到对应 patch：

```text
patch_x = floor(x_2d / patch_size)
patch_y = floor(y_2d / patch_size)
detail_i += patch_detail[patch_y, patch_x]
```

更稳的方式是用 Gaussian 对 patch 的实际渲染贡献作为权重：

```text
detail_i += sum_p w_{i,p,v} * patch_detail_{p,v}
detail_count_i += sum_p w_{i,p,v}
avg_detail_i = detail_i / detail_count_i
```

其中：

```text
i：第 i 个 Gaussian
p：patch
v：训练视角
w_{i,p,v}：该 Gaussian 在该视角、该 patch 的 alpha / contribution 权重
```

这样一个 Gaussian 如果经常投影到高误差、高边缘、高语义重要性的 patch，它的 `avg_detail_i` 就会变大。

## 6. 怎么接入 clone / split

不建议做成：

```text
高 detail patch 里的所有 Gaussian 全部 clone
```

这样很容易导致 Gaussian 数量暴涨，也可能把 marine snow、噪声、动态物体放大。

更稳的做法是保留原版 3DGS 的梯度标准，只是用 patch_detail 调节 threshold。

方式一：放大梯度

```text
adjusted_grad_i = grad_i * (1 + lambda_detail * avg_detail_i)

if adjusted_grad_i > base_threshold:
    clone_or_split(i)
```

方式二：降低高细节区域的阈值

```text
threshold_i = base_threshold / (1 + lambda_detail * avg_detail_i)

if grad_i > threshold_i:
    clone_or_split(i)
```

直观理解：

```text
同样的梯度下，
如果一个 Gaussian 经常落在重要且细节丰富的 patch，
它更容易被 densify。
```

clone / split 的选择仍然可以沿用原版逻辑：

```text
scale 小：jittered clone
scale 大：split
```

## 7. 怎么接入 prune / reallocation

patch_detail 不只可以指导加球，也可以指导删球。

低 detail、低贡献、低 opacity 的 Gaussian 可以更积极 prune：

```text
prune_score_i =
    low_opacity_i
    * low_contribution_i
    * (1 - avg_detail_i)
```

也就是说：

```text
高 detail 区域：prune 更保守
低 detail 区域：prune 更积极
```

如果要做 reallocation，可以把低重要性区域 prune 掉的 Gaussian budget 分配给：

```text
adjusted_grad 高
avg_detail 高
当前 render error 高
```

的区域。

## 8. 新 Gaussian 的位置从哪里来

不要直接在 2D patch 上“凭空放一个 Gaussian”。patch 只是告诉我们哪个区域需要更多容量，真正的新 Gaussian 位置应该从已有 3D Gaussian 来。

推荐规则：

```text
1. 找到投影到高 detail patch 的已有 Gaussian
2. 根据这些 Gaussian 的 3D 位置生成新 Gaussian
3. 小 Gaussian 用 jittered clone
4. 大 Gaussian 用 split
```

只有在高 detail patch 里几乎没有可用 Gaussian 时，才考虑用深度反投影生成新 3D 点：

```text
pixel / patch center + depth -> camera ray back-projection -> world point
```

但这个方案依赖 COLMAP depth、当前渲染 depth 或额外 depth 网络，误差风险更大，第一版不建议上来就做。

## 9. 最小实现路线

### V0：不用语义模型的 baseline

先只做：

```text
patch_detail = patch_error * (1 + alpha * patch_edge)
```

然后用它调节 densification threshold。

目的：验证 patch-aware densification 是否比原版梯度 densification 更好。

### V1：加入语义重要性

用 segmentation / SAM / Grounded-SAM / CLIPSeg 得到重要目标 mask，再计算：

```text
patch_detail =
    patch_error
    * (1 + alpha * patch_edge)
    * (base + (1 - base) * semantic_importance)
```

目的：让 coral、shipwreck、object 等区域更容易拿到 Gaussian budget。

### V2：多视角累计

不要只看单张图，而是把每个 Gaussian 在多个训练视角里的 detail 累计起来：

```text
avg_detail_i = total_detail_i / total_contribution_i
```

这样可以减少单视角误判。

### V3：reallocation

加入更明确的 budget 分配：

```text
低 detail + 低贡献 Gaussian prune
高 detail + 高误差 Gaussian densify
```

这一步才更接近“把 Gaussian 从不重要区域重新分配到重要区域”。

## 10. 实验验证

不能只看训练视角 PSNR，因为多加 Gaussian 可能只是过拟合训练图。

需要看：

```text
held-out view PSNR
held-out view SSIM
held-out view LPIPS
高细节目标边界是否更清晰
Gaussian 总数量是否可控
训练时间是否明显增加
是否出现更多漂浮点 / 噪声点
```

建议至少对比：

```text
1. 原版 3DGS
2. error + edge patch-aware densification
3. error + edge + semantic patch-aware densification
4. 只加 semantic feature loss 的方法
```

如果第 2 个就明显有效，说明核心收益可能来自 patch-aware densification；如果第 3 个进一步提升，说明语义先验确实有用。

## 11. 当前结论

这个方向是可行的，但关键点不是“给每个 Gaussian 加语义特征”本身，而是让语义 / patch 信息真正影响 Gaussian 的数量分配。

最稳妥的论文创新点可以表述成：

```text
Semantic-aware patch-guided densification for underwater 3D Gaussian Splatting.
```

更具体一点：

```text
利用图像误差、局部边缘复杂度和语义重要性构造 patch-level detail score，
再将该 score 累计到可见 Gaussian 上，
动态调节 3DGS 的 clone / split / prune 策略，
使 Gaussian budget 从低价值区域转移到高细节目标区域。
```

这个方案比单纯加一个语义 MSE loss 更直接，因为它会改变最终 3D 表示的结构和容量分布。
